expr : term PLUS term*
     | term MINUS term*

term : factor MUL factor*
     | factor DIV factor*

factor : INT
       | FLOAT
       | factor PLUS factor
       | factor MINUS factor
       |LPAREN expr RPAREN
